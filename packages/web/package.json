{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@models.dev/web",
  "scripts": {
    "dev": "bun run --hot ./src/server.ts",
    "build": "./script/build.ts",
    "start:local": "bun run ./src/server.local.ts"
  },
  "dependencies": {
    "hono": "^4.8.0",
    "models.dev": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.2.16"
  }
}
